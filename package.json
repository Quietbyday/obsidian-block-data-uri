{
	"name": "block-data-uri",
	"version": "0.1.3-beta",
	"description": "Strips data URI image links from pasted text",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"devDependencies": {
		"builtin-modules": "3.3.0",
		"esbuild": "^0.28.0",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "^4.7.4"
	}
}
